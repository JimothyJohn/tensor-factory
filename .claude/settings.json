{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git worktree list 2>/dev/null; printf 'Parallel/concurrent work? Give the task its own worktree: scripts/worktree <branch>. Never share a checkout (one HEAD/index/.venv/port). See CLAUDE.md -> Parallel work.\\n'"
          }
        ]
      }
    ]
  }
}
