{
  "_comment": "Copy to .mcp.json to register the tensor-factory detector as an MCP server for this project.",
  "mcpServers": {
    "tensor-factory": {
      "command": "uv",
      "args": ["run", "tensor-factory-mcp"]
    }
  }
}
