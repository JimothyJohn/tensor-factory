[project]
name = "helicoils-train"
version = "0.1.0"
description = "Train the tiny helicoil detector and export an int8 ONNX model"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "helicoils",
    "numpy>=1.24",
    "pillow>=10",
    "onnxruntime>=1.17",
]

[project.optional-dependencies]
# torch is the only heavy bit. Pull for a training run with
# `uv pip install torch` (or sync this extra) on a CUDA box or this Mac's MPS.
train = [
    "torch>=2.2",
]

[project.scripts]
helicoils-train = "helicoils_train.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/helicoils_train"]

[tool.uv.sources]
helicoils = { workspace = true }
