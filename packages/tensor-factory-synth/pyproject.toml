[project]
name = "tensor-factory-synth"
version = "0.1.0"
description = "Synthetic image generation (Nano Banana / Gemini) and auto-labeling (GroundingDINO) for tensor-factory"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "tensor-factory",
    "pillow>=10",
]

[project.optional-dependencies]
# Generation backend: hosted Gemini API (Nano Banana). Light, no GPU. Pull with
# `uv sync --extra gemini`; needs a GEMINI_API_KEY in the environment.
gemini = [
    "google-genai>=1",
]
# Auto-labeler (GroundingDINO) only. Heavy, GPU-shaped; NOT installed by default
# `uv sync`. Pull with `uv sync --extra gpu` on a CUDA box or this Mac Studio's MPS.
gpu = [
    "torch>=2.2",
    "transformers>=4.44",
    "accelerate>=0.30",
]

[project.scripts]
tensor-factory-synth = "tensor_factory_synth.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tensor_factory_synth"]

[tool.uv.sources]
tensor-factory = { workspace = true }
